const messages = {
    EMAIL_CONFLICT: 'Email is already used',
    INCORRECT: 'Email or password is incorrect',
    USER_CONFLICT: 'User not found',

    CAR_ID_CONFLICT: 'Car not found',
    OWNER_CONFLICT: 'Owner not found',

    TOKEN_CONFLICT: 'Token not found',
    TOKEN_TYPE: 'Invalid tokenType',
    INVALID_TOKEN: 'Invalid token',
};

module.exports = messages;
